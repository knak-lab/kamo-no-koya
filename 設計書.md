# カモの小屋 収益分析アプリ 設計書(実装用・最終版)

Claude Codeでの実装にそのまま渡すための設計書。アーティファクトで検証済みのロジック・画面構成と、既存GASスクリプトとの整合方針をすべて反映している。

---

## 0. 全体方針

| 項目 | 方針 |
|---|---|
| スプレッドシート | **既存の収益管理シートをそのまま使用**。列・シートの追加のみ行い、既存列のリネーム・削除・型変更はしない |
| 既存GASスクリプト(`syncFromSquare`) | **一切変更しない**。`syncOrders()`(売上_Squareへの取込)・`syncCatalog()`(Square→商品マスターへの上書き)は今まで通り毎日18時に動かし続ける |
| AppSheet | 移行期間中は並行稼働可 |
| Square(POS・決済端末) | 変更なし。決済端末は継続利用 |
| 売上データの正 | **Square**。既存の`売上_Square`シートをそのまま読み取り専用で利用する(新規に作り直さない) |
| 商品マスタの正 | **フェーズ制**(詳細は12章)。当面はSquareが正、運営に慣れたタイミングで手動切替 |
| 原価の扱い | 売上取込時点の商品マスタ原価を`unitCostAtSale`としてスナップショット保存。以後変わらない |
| 認証情報 | Square Access Tokenは`PropertiesService`のスクリプトプロパティに保存し、コードに直書きしない(既存スクリプトも将来的に移行を推奨) |

---

## 1. 既存GASスクリプトとの共存ルール

既存の`syncFromSquare()`(`syncOrders()` + `syncCatalog()`)は触らない前提で、新アプリ側は以下のように棲み分ける。

| シート | 既存スクリプトの動き | 新アプリの扱い |
|---|---|---|
| `売上_Square`(id, 日付, 商品名, 数量, 金額(円), 支払方法, 取得日時) | `syncOrders()`が毎日18時に追記(7列固定) | **そのまま読み取り専用で使用**。原価スナップショット列(`unitCostAtSale`, `costSubtotal`)は8列目以降に**追加**する。既存スクリプトは7列しか書き込まないため、追加列に影響なし |
| `商品マスター`(id, 商品名, 価格(円), カテゴリ, 更新日時) | `syncCatalog()`が毎日18時に**全件上書き** | アプリ独自の商品マスタは**別シート**「商品マスター_原価管理」に置く(レシピ・原価計算・kind等を持つ)。既存シートとは完全分離し、上書きの影響を受けない |

新アプリ側で追加する後処理関数(既存関数とは独立):
```
function stampCostSnapshot() {
  // 売上_Squareの新規行(8列目が空の行)を対象に、
  // 商品名で「商品マスター_原価管理」を突き合わせ、
  // その時点の原価を unitCostAtSale / costSubtotal として書き込む
}
```
`syncOrders()`実行後にトリガーで呼ぶ(既存関数の中身は変更しない、別関数として追加)。

---

## 2. Square API 認証情報

| キー | 値 | 備考 |
|---|---|---|
| `SQUARE_APP_ID` | `sq0idp-eT45cHJ1KwEQ_2w9zUXhvA` | 公開情報 |
| `SQUARE_ACCESS_TOKEN` | (非公開・スクリプトプロパティ) | 露出した場合は速やかにDeveloper ConsoleでReplaceして再発行する |
| `SQUARE_LOCATION_ID` | `L32246QC` | カモの小屋(Main) |

---

## 3. データフロー全体図

```
[Square Catalog] ←(フェーズ2以降・即時upsert)── [新GAS] ──(既存syncOrders・18時)──→ [売上_Squareシート]
                                                    │                                      │
                                          [商品マスター_原価管理]                    [stampCostSnapshot]
                                                    │
                                              [Reactフロント]
```

---

## 4. スプレッドシート構成

### 4.1 既存シート(変更なし・読み取りのみ利用)
- `売上_Square`(既存7列 + 追加2列: `unitCostAtSale`, `costSubtotal`)
- `商品マスター`(既存。参照のみ、アプリからは編集しない)

### 4.2 新規シート
| シート名 | 内容 |
|---|---|
| 商品マスター_原価管理 | id, name, price, kind(single/set), squareCatalogId, squareCatalogVersion |
| レシピ | productId, servings, ingredients(可変長), packaging(可変長) |
| セット内訳 | productId(セット), componentProductId(単品), qty |
| 材料・包材マスタ | id, name, category(原材料/包材), unit(g/個), unitPrice |
| 販売先マスタ | id, name, rate(リベート率), memo |
| 販売形態マスタ | id, name, rebateApplicable |
| 経費マスタ(時間単価) | item(店舗利用料(製造・販売)/店舗利用料(製造)/人件費), hourlyRate |
| 経費 | id, date, item, amount, hours(時間単価項目のみ) |
| 日次設定 | date, channelId, clientId |
| 月次目標 | yearMonth, salesBudget, grossMarginRatio(%), profitBudget |
| TODO | id, category(たくらみ/みせづくり/汗かき/経営・管理), task, deadline, status, snoozed |
| サブタスク | id, parentTaskId, name, assignee, deadline, status, snoozed, sortOrder |
| Square同期ログ | timestamp, type(import/sync), status, message |

---

## 5. 商品原価計算ロジック

### 5.1 単品商品
```
製造原価計   = Σ(材料.数量 × 材料.仕入単価)
製造原価単価 = 製造原価計 ÷ 分割数
包材費計     = Σ(包材.数量 × 包材.仕入単価)
原価         = 製造原価単価 + 包材費計
原価率       = 原価 ÷ 価格
限界利益率   = (価格 − 原価) ÷ 価格
```

### 5.2 セット商品(2パス計算・単品を先に計算)
```
材料費 = Σ(構成商品ごと: 単品原価 × 数量) + Σ(梱包・資材ごと: 資材単価 ※数量は掛けない)
包材費 = セット自身のレシピ(あれば)の包材費計
原価   = 材料費 + 包材費
```
構成商品として選べるのは単品のみ。

### 5.3 価格の自動計算
```
価格 = CEIL( 原価 ÷ (原価率 ÷ 100) / 50 ) × 50   ※50円単位切り上げ、価格の直接編集でも上書き可
```

---

## 6. 売上原価のスナップショット

```
取込時: unitCostAtSale = その時点の商品原価
        costSubtotal   = unitCostAtSale × qty
以後、原価が変わっても costSubtotal は再計算しない
```

---

## 7. 経費・時間単価の自動計算

「店舗利用料(製造・販売)」「店舗利用料(製造)」「人件費」の3項目は時間単価管理。
```
経費金額 = 入力した時間(h) × 経費マスタの時間単価(円/時)
```
それ以外の項目は金額を直接入力。

---

## 8. 日次・月次集計ロジック

```
客数         = 当日の売上のユニーク伝票ID数
売上(値引前) = Σ 当日の売上.amount
リベート     = IF(販売形態がリベート対象 AND 委託先選択済み, 売上(値引前) × リベート率, 0)
売上(日次)   = 売上(値引前) − リベート
客単価       = 売上(日次) ÷ 客数
原価(標準)   = Σ 当日の売上.costSubtotal
粗利         = 売上(日次) − 原価(標準)
原材料仕入   = Σ 当日の経費[項目="原材料・資材仕入"].amount
その他経費   = Σ 当日の経費[項目≠"原材料・資材仕入"].amount
営業利益(管理) = 粗利 − その他経費
原価差異     = 原材料仕入 − 原価(標準)
営業利益(財務) = 営業利益(管理) − 原価差異
```
月次集計は日次集計を年月ごとに合算。

---

## 9. サマリタブ

商品原価(標準)ベース。4種のグラフダッシュボード + 詳細データ(開閉式)。

| # | グラフ | 内容 |
|---|---|---|
| 1 | 月次予実 | 棒グラフ(目標・実績、縦=金額・横=月)。指標フィルタ(売上/営業利益)・販売形態フィルタ。売上選択時は実績粗利率(実線)・目標粗利率(破線)を右軸(%)に重畳 |
| 2 | 日次予実 | 棒グラフ(実績のみ、縦=金額・横=日)。同フィルタ。売上選択時は実績粗利率を右軸に重畳 |
| 3 | 客数と客単価 | 客数=棒グラフ(左軸)、客単価=折れ線(右軸)、横軸=日付 |
| 4 | 商品別売上(上位10) | 横軸=商品名、売上金額の棒グラフ |

- 棒グラフは黒字=青(`#2563eb`)、赤字=赤(`#dc2626`)
- 販売形態を絞り込むと、目標系(予算は形態別に持たないため)は非表示

---

## 10. PLタブ

実際の入出金ベース、損益計算書形式(縦積み・月ごと)。

```
売上高
売上原価(原材料・資材仕入) ▲
─────────────
売上総利益
販売費及び一般管理費 ▲
─────────────
営業利益
(参考)原価差異
```

---

## 11. 目標(予算)入力

入力タブに配置。月単位・登録→一覧追加方式(一覧は開閉式・行ごとに編集ボタン)。

| 項目 | 入力方法 |
|---|---|
| 売上目標(円) | 直接入力 |
| 粗利率目標(%) | 直接入力 |
| 粗利目標(円) | 自動計算(売上目標 × 粗利率目標) |
| 利益目標(円) | 直接入力 |

---

## 12. Square連携(フェーズ制)

商品マスタの「正」を段階的に切り替える。現場の混乱を避けるため、切替は手動。

| フェーズ | 商品マスタの正 | 動き |
|---|---|---|
| フェーズ1(当面) | Square | 既存`syncCatalog()`は変更なしで稼働継続。アプリの「商品マスター_原価管理」は、価格・商品名をSquare側(既存`商品マスター`シート)から一方向で反映。アプリはレシピ・原価だけを追加入力し、原価率等は参考表示に留め、価格には反映しない |
| 切替操作 | ー | マスタタブ最下部に手動ON/OFFトグルを設置。切替時は必ず確認ダイアログ「Squareとの商品マスタの連動が変わります。本当に実行しますか?」を表示し、実行/キャンセルを選択するまで反映しない |
| フェーズ2(切替後) | アプリ | 価格・レシピの保存タイミングでCatalog APIに即時upsert(アプリ→Square)。Square側での価格変更は想定しない運用に切替 |

### 12.1 フェーズ2でのCatalog upsert仕様
- `squareCatalogId`が空なら新規作成、あればversionを確認してupsert
- 失敗時は「Square同期ログ」に記録、自動リトライなし(手動再実行ボタン)
- セット商品もSquare上は通常の1商品として登録

---

## 13. TODOタブ

元のAppSheet(`todo`/`サブタスク`テーブル)の構成を踏襲。

| 項目 | 内容 |
|---|---|
| タスク | カテゴリ(たくらみ/みせづくり/汗かき/経営・管理)ごとにグループ表示。タスク名・期限・ステータス(未着手/進行中/完了) |
| サブタスク | サブタスク名・担当(スタッフ選択式)・期限・ステータス。同一タスク内で↑↓ボタンにより並び替え可能 |
| ちょっとあと | タスク・サブタスクそれぞれに「ちょっとあと」ボタン。押すと`snoozed=true`になり、一覧からデフォルト非表示になる。画面右上の🙈/👁ボタンで表示・非表示を切り替え(デフォルト🙈=非表示) |

---

## 14. 画面構成(React・タブ)

| タブ | 内容 |
|---|---|
| **入力** | 目標(月次) → 日次設定(販売形態・委託先) → 経費入力(時間単価項目は時間入力で自動計算)。売上入力は無し |
| **サマリ** | 9章の4グラフ + 詳細データ(開閉式) |
| **PL** | 10章の損益計算書形式 |
| **TODO** | 13章の内容 |
| **マスタ** | レシピ編集(商品検索コンボボックス、単品/セット切替、原価率から価格自動計算) → 商品マスター一覧(開閉式) → 販売先マスタ(開閉式) → 経費マスタ・時間単価(開閉式) → 材料・包材マスタ(開閉式) → Square連携(ON/OFFトグル+確認ダイアログ) |

---

## 15. 実装フェーズ(目安)

| フェーズ | 内容 |
|---|---|
| 1 | GAS基盤: 既存スプレッドシートへの追加列・新規シート作成、Web API化(CRUD)。既存`syncFromSquare`には触れない |
| 2 | 商品マスタ(原価管理)・レシピ・セット内訳・材料マスタのGAS実装 |
| 3 | 売上_Square読み取り + `stampCostSnapshot`(原価スナップショット後処理) |
| 4 | 日次・月次集計ロジックのGAS実装 |
| 5 | 目標・経費・TODOのGAS実装 |
| 6 | React画面実装(5タブ) |
| 7 | Square連携フェーズ1(Square→商品マスター_原価管理の一方向反映) |
| 8 | 通し確認・実データ検証 |
| 9(将来) | Square連携フェーズ2切替(アプリ→Square Catalog upsert)。ON/OFFトグルの実装含む |

---

## 16. 未確定事項

- Square Orders APIのページング・レート制限対応
- 返金・キャンセル発生時の売上取込の扱い
- `stampCostSnapshot`実行タイミング(`syncOrders()`直後にトリガーで自動起動 or 手動ボタン)
- 「Square同期ログ」の保持期間・エラー通知方法
- 日次・月次集計をGAS側で都度計算するか、シートにキャッシュするか
- フェーズ2切替後、Square側での価格直接編集をどう防ぐか(運用ルールのみか、Square側の権限制限も検討するか)
